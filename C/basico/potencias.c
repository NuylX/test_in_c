#include <stdio.h>

int main() {
    int x = 2;
    int quadrado = x * x;
    int cubo = x * x * x;

    printf("O quadrado de %d é: %d\n", x, quadrado);
    printf("O cubo de %d é: %d\n", x, cubo);

    return 0;
}